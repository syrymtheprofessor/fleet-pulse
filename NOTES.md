NOTES.md

1) Я бы добавил Spring Security, потому сейчас API каждому открыт.

2) Пытался бы способ уменьшить интервал о подсчитывании GPS (_с 5 минут до 30 секунд хотябы_).

3) Обязательно метрику по 2 пункту, чтобы понять как система справляется с нагрузкой.

3) Чтобы ускорить движение данных с БД, однозначно индекс поставил бы. 
Вместо String 'id', на Long или UUID (возможно +'ts' тоже).

На TelemetryConsumer рассмотрел бы Protobuf вместо JSON для уменьшения размера сообщений в RabbitMQ.

4) ?Не понял логику отчистку trips, поездка считается со старта до конца первого trip. 
Но этих поездок и несколько же может быть. Сортировку по дату и времени добавил бы (связав с индексами). 

5) Очистку кэша. Добавить TTL, ?если необходимо. Сперва мониторить/поставить метрики, 
чтобы понять реальную эффективность и подобрать TTL под данные, а не наугад.
